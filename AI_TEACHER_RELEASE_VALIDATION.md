# AI Teacher 与管理能力升级验收

验收日期：2026-09-05。范围：本地 Full 容器部署、公开内容边界、管理配置同步与真实教学操作。

## 构建与回归

- API、Teacher、Validator、Web 四个镜像实际构建成功；八个 Full 服务容器正常运行。
- 完整 `npm test`、`npm run typecheck:web`、模型控制面专项测试与 `git diff --check` 通过。
- 数据库升级前完成备份；旧资源策略版本保留，新增任务生命周期表正常使用。
- API/Teacher Internal Token 和 Tool Token 分别匹配且彼此不同。实际 Validator Tool 无认证返回 401，正确认证返回官方校验通过。

## 管理能力与真实模型

- 四个模型部署均具有匹配当前配置的真实能力探测记录，声明能力全部通过；模型配置 Desired、Applied、Observed 一致。
- 资源策略 Schema 24 的 45 个参数完成迁移与回读，Teacher、Provider、Probe、Validator 四个 Owner 已应用。
- 本地对比源环境生效资源参数、对应模型路由和等级额度，目标配置一致。环境地址、数据库及服务凭据保持独立；这些运行配置不属于提交内容。
- 新的三轮 Run Checkpoint 均记录同一 Active 策略版本和 checksum。

## 真实 Chrome 三轮验收

学生代理仅获得页面可见内容，自主提出后续问题。

| 轮次 | 学生目标 | 实际结果 | 用时 |
| --- | --- | --- | --- |
| 1 | 理解 part def 与 part | 解释定义、usage 与值的区别 | 54.96 秒 |
| 2 | 获得 Battery、Motor、Vehicle 完整模型 | 官方校验通过；写入编辑器后再次校验并生成视图 | 64.11 秒 |
| 3 | 新增 controller，保留 battery 与 motor | 保留原结构，新增 Controller 定义及 controller usage；写入编辑器后再次校验并生成视图 | 65.37 秒 |

独立学生确认本次学习目标完成。当前页面通过复制完整候选并粘贴到编辑器应用模型，不提供一键应用入口。官方 Validator PASS 仅证明对应模型的语法与语义，不证明工程设计正确性。

## 验收发现与修复

1. Validator 镜像缺少资源策略包：补齐构建和运行阶段 COPY。
2. 老策略 reasoningPolicy 字段遗漏迁移：增加旧字段映射，显式新字段保持优先，并增加迁移回归。
3. AI 面板翻译资源遗漏：补齐所用中英文命名空间，容器页面复查通过。
4. GLM 结构化输出探测达到原 Token 上限：保留 inconclusive 记录，将该项上限调整为 512；总预算为 1184，管理页从服务端读取确认范围，不放宽输出校验。
5. 模型管理刷新后流程条错误显示待发布：改为依据实际生效状态；允许对发生 drift 的 Active 配置重新同步，服务端原有能力与发布门禁保留。

Chrome 原生确认弹窗自动控制曾超时，首轮能力探测使用同一服务端受限探测程序执行并保存审计；不能据此宣称原生弹窗自动交互通过。后续管理页真实能力记录与教学交付分别验收。

本记录不覆盖远程部署、生产流量、长时间压力测试或数据库备份恢复演练。
